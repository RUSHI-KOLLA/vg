# Core — required for web API
groq>=0.9.0
requests>=2.31.0
aiolimiter>=1.1.0
python-dotenv>=1.0.1
tenacity>=8.2.3

# Optional — web search (lazy-imported, graceful fallback if missing)
tavily-python>=0.3.5
