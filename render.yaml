services:
  - type: web
    name: vg-intelligence
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: ln -sfn . vg && PYTHONPATH=. python -m vg.main --web --port $PORT
    envVars:
      - key: TAVILY_API_KEY
        sync: true
